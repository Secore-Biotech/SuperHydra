"""Configuration namespace for vol_event_persistence.

Immutable probe constants are in pre_lock.py. Future runtime config
(e.g. database connection, fixture paths) would land here under
separate modules; pre_lock.py is reserved for spec-frozen parameters.
"""
