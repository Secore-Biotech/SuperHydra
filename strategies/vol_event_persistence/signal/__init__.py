"""Signal computation for vol_event_persistence.

Pure functions over canonical input types. No I/O, no clocks, no global
state — same property that makes A1's signal layer paper-Sharpe
reproducible.
"""
